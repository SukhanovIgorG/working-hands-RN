import { Text } from 'react-native';
import { DefaultLayout } from '../../layouts/default';

const HomeScreen = () => {
  return (
    <DefaultLayout>
      <Text>Home</Text>
    </DefaultLayout>
  );
};

export default HomeScreen;
